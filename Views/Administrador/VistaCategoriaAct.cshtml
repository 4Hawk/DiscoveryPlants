@model Categorias;
@{
    var item = (Categorias)ViewBag.CategoriaE;
}

        <div class="content py-5 px-4 mx-3 my-2">
            <div class="row">
            
                <h2 class="title">Ingrese nuevo nombre de categoría<hr></h2>
            </div>
            <div class="body">
                <form method="post" asp-controller="administrador" asp-action="actualizarcategoria" class="col-md-8">
                    <div class="form-group"> 
                        <label for="form16" asp-for="Nombre"></label> 
                        <input type="text" class="form-control" id="form16" value="@item.Nombre" asp-for="Nombre">
                    </div>
                    <div class="py-3"><button class="btn btn-primary py-3" asp-controller="administrador" asp-action="actualizarcategoria" asp-route-codigo="@item.Id">Confirmar</button></div>
                </form>
            </div>
        </div>

                            